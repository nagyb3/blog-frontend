// TODO adjust these to the actual backend schema (this is just a placeholder)
export type TPost = {
  id: number;
  title: string;
  content: string;
  author: string;
  createdAt: string;
  updatedAt: string;
};
